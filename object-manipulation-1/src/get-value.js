/* exported getValue */

/** pseudocode for getValue
 * 1. make the function definition for getValue() with two paramethers: object and key;
 * 2. return from the function the value of the key paramether property of the object paramether.
 */

function getValue(object, key) {
  return object[key];
}
