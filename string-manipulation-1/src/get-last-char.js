/* exported getLastChar */

/** pseudocode for getLastChar
 * 1. make the function definition for getLastChar() with 1 paramether: string;
 * 2. return the last character of the string.
 */

function getLastChar(string) {
  return string.at(string.length - 1);
}
