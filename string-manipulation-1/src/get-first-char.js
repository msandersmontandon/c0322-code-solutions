/* exported getFirstChar */

/** pseudocode for getFirstChar
 * 1. make the function definition for getFirstChar() with 1 paramether: string;
 * 2. return the first character of string.
 */

function getFirstChar(string) {
  return string.at(0);
}
