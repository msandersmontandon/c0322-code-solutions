/* exported last */

/** pseudocode for last
 * 1. make the function definition for last() with 1 paramether: array;
 * 2. return the value of the last item in the array paramether.
 */

function last(array) {
  return array[array.length - 1];
}
