/* exported defaults */

/** pseudocode for default
 * 1. make the function definition for default() with 2 paramethers: target and source;
 * 2. use a for in loop to iterate through the sourceKey property names of the source paramether;
 * 3. use another fo in loop to iterate through the targetKey property names of the property paramether;
 *
 */
