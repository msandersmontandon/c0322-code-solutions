/* exported setValue */

/** pseudocode for setValue
 * 1. make the function definiton for setValue() with 3 paramethers: object, key and value;
 * 2. assign the value paramether as the value of the key paramether property of the object paramether.
 */

function setValue(object, key, value) {
  object[key] = value;
}
